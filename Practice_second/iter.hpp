template<class T1, class T2> // Шаблон, тип данный определяется при подстановке переменных во время вызова функции
void iter(T1 *array, int size, T2 (*foo)(T1 array)) {
	for (int i = 0; i < size; i++) {
		
	}
}